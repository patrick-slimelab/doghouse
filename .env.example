# Git ref (commit SHA / tag) to run
MOLTBOT_REF=01e0d3a320252664dc2bdeafdacb96cb4a473be0

# Where Scoob should reach the host ooba API
OPENAI_BASE_URL=http://host.docker.internal:5000

# Optional: pin port for any exposed web UI (if Scoob runs one)
# DOGHOUSE_PORT=xxxx
